# Gap List — suite audit vs the Test Model (FR-014/SC-008)

**Feature**: 002-test-model | **Audited**: 2026-06-21

One row per model-vs-current-suite deficiency. `status = closed` required at feature end.
**Final open-gap count: 0** — every row is closed (fixed or verified-no-gap).

| # | Location | Kind | Target layer | Resolution | Status |
|---|----------|------|--------------|------------|--------|
| 1 | `utils/THttpClientSpec.scala` | ad-hoc `com.sun.net.httpserver` | Unit (ScalaMock) | Extracted `HttpGetter` trait + `THttpClient.withTransport` seam; rewrote with ScalaMock `mockFunction` transport, captured-request assertions | **closed** |
| 2 | `feeders/HttpJsonFeederSpec.scala` | ad-hoc `com.sun.net.httpserver` | Unit (ScalaMock) | Added `HttpJsonFeeder.fetch(HttpGetter, …)` seam; rewrote mocking `HttpGetter`, exact-record + request assertions | **closed** |
| 3 | `redis/RedisActionSpec.scala` | (seed claimed empty bodies) | Unit | **Verified — no gap**: already asserts real session values (`updateSessionWithResult` 3 cases incl. negative). No empty `in {}` bodies | **closed (verify-only)** |
| 4 | `redis/RedisActionBuilderSpec.scala` | (seed claimed assertion-free) | Component/Unit | **Verified — no gap**: ~50 cases assert builder types, `saveAs`/`requestName`, and Validation success/failure (negative cases). No empty bodies | **closed (verify-only)** |
| 5 | `src/it/redis/RedisIntegrationSpec.scala` | already present | External integration (Testcontainers) | **Verified — no gap**: redis:7-alpine `ForAllTestContainer`, strict exact-value asserts across String/Counter/Hash/List/Key/Set | **closed (verify-only)** |
| 6 | `src/it/feeders/VaultIntegrationSpec.scala` | already present | External integration (Testcontainers) | **Verified — no gap**: vault:1.17 real container; also exercises `THttpClient` over the real wire (covers the wire path deferred from unit) | **closed (verify-only)** |
| 7 | `utils/jwt/JwtSpec.scala`, `utils/jwt/JwtKeysSpec.scala` | (constitution previously over-mandated Testcontainers) | Unit (real crypto) | **Verified — no gap**: real RSA/HMAC keys, real sign/verify, asserts real claims. JWT is pure crypto → no container needed (constitution §III corrected) | **closed (verify-only)** |
| 8 | `diagnostics/UtilityIntegrationSpec.scala`, `diagnostics/DiagnosticsSpec.scala` | (constitution previously over-mandated Testcontainers) | Unit (real JVM/console state) | **Verified — no gap**: asserts deterministic banner structure + disabled-diagnostics empty output; no env-dependent exact values | **closed (verify-only)** |
| 9 | `profile/http/*` | (seed listed as HTTP-emitting) | Component/compile + e2e | **Verified — no gap**: `HttpProfileConfig`/`HttpRequestComponents`/`HttpRequestConfig` build request CONFIG, do not emit HTTP (no `THttpClient`/`java.net.http`). Wire path is the e2e sim; build logic in `ProfileBuilderTest` | **closed (verify-only)** |
| 10 | `storage/StorageBackend.scala` → `JdbcStorageBackend` | missing real-DB integration; only the `RecordingJdbcDriver` JDK-proxy fake (`JdbcTestSupport`) exercised it | External integration (Testcontainers) | **DONE (user authorized "Fix DDL + Postgres" 2026-06-21)**: changed the identity DDL `BIGINT AUTO_INCREMENT` → portable `BIGINT GENERATED BY DEFAULT AS IDENTITY` (Postgres/H2 standard SQL); added `org.postgresql:postgresql:42.7.4` (`it` scope only); new `src/it/storage/JdbcStorageIntegrationSpec` drives `save`/`load`/`clear` against a real `postgres:17-alpine` container asserting exact values + insertion order + empty boundary. 4 tests green. | **closed** |
| 11 | `examples/scala-sbt-example` | no meaningful e2e of picatinny features over real HTTP | Full Gatling e2e (overlay) | Covered by overlay sim `HttpIntegrationCoverage` (decomposed `scenarios/`→`cases/`→`feeders/`) driving picatinny features over REAL HTTP against a WireMock server that ECHOES request values back via response templating. Each feature via its picatinny method: feeders (`CurrentDateFeeder`), JWT (`setJwt`), transactions (`startTransaction`/`endTransaction`), converters (`IntensityConverter` `.rpm` for the injection rate). Gatling `check` validates the RESPONSES (jsonPath `$.ts` == the `CurrentDateFeeder` value, jsonPath `$.auth` == `"Bearer #{jwt}"`) plus `.assertions(global.failedRequests.count.is(0), details("api-call")…)`. The rule that keeps it from being mock-testing-mock: assert RESPONSES with `check`, NEVER `WireMock.verify` of the request and NEVER re-decode the request (the JWT's crypto correctness is unit-tested separately in `JwtSpec`). WireMock lives in the example overlay's TEST scope only (injected by `scripts/test-scala-sbt-template.sh`), never a library dependency. Run by `sbt Gatling/test` under the template-tests CI gate. | **closed** |
| 12 | `javaapi/redis` facade | no Redis facade parity test | Facade delegation | Add facade delegation/parity coverage for the Redis facade alongside feeders/profile/utils | **closed** (see T030) |
| 13 | `feeders/EnvFeeder.scala` | no unit coverage | Unit | **DONE**: added `EnvFeederSpec` asserting real env-backed feeder values | **closed** |
| 14 | `config/SimulationConfig.scala` | no unit coverage | Unit | **DONE**: added `SimulationConfigSpec` asserting real config-load values | **closed** |
| 15 | `feeders/SeparatedValuesFeeder.scala` | no unit coverage | Unit | **DONE**: added `SeparatedValuesFeederSpec` asserting real parsed records | **closed** |
| 16 | `assertions/AssertionsBuilder.scala` | not unit-testable (no injectable `GatlingConfiguration`) | Unit | **DONE**: REFACTORED to take an implicit `GatlingConfiguration` so it is unit-testable; added `AssertionsBuilderSpec`. The Java facade `Assertions.java` cannot delegate to it (Gatling's Java `Assertion` wrap constructor is package-private), so it keeps the Java DSL impl, documented | **closed** |

## Open-gap summary

- **Open-gap count: 0.** All 16 rows are `closed` (fixed or verified-no-gap). #10 (JDBC
  real-DB container) was closed after the user authorized the DDL fix + Postgres driver.
  #11 (WireMock full-Gatling e2e) is closed by the `HttpIntegrationCoverage` overlay sim
  (assert RESPONSES via `check`, never `verify`/re-decode the request). #13–#16 closed
  newly-found unit gaps (EnvFeeder, SimulationConfig, SeparatedValuesFeeder, and the
  AssertionsBuilder refactor that made it unit-testable).
- The original seed's "empty Redis bodies" and "JWT/diagnostics need Testcontainers"
  items were **stale** — the current suite already covers those with real values; the
  constitution over-specification that caused the JWT/diagnostics confusion is fixed (§III).
