# Verification Records

A claim is `verified` only after re-checking the PRIMARY source (not the
curated summary). Verdicts: verified | refuted | unverifiable.

| ID | Claim | Method | Verdict | Confidence | Evidence | Date |
|----|-------|--------|---------|------------|----------|------|
| V001 | Long-span predicate (`span >= 0 && span < Long.MaxValue`, bound `span+1`) is branch- and bound-equivalent to the BigInt classification for all min<=max (research R2) | Executable refutation: jshell sweep, 78 corner pairs + 2M random pairs vs BigInteger reference | verified | high | E001 | 2026-07-21 |
| V002 | `s"$prefix$key"` in `prefixKeys` compiles to StringBuilder "interpolation machinery" whose removal (`prefix + key`) reduces per-key allocation (issue #130 premise; research R5 decision) | Disassembled production classfile `Syntax$FeederOps$.class` (`$anonfun$prefixKeys$2`): single `invokedynamic makeConcatWithConstants` — same lowering `+` would produce | refuted | high | E002 | 2026-07-21 |
| V003 | `String.toLowerCase` is locale-sensitive with special casing a per-char `Character.toLowerCase` loop cannot reproduce (research R3) | jshell: U+0130 → 2 codepoints (69,307) via String vs 1 (69) via Character; tr-locale 'I' → 0x131 vs ROOT 0x69 | verified | high | E003 | 2026-07-21 |
| V004 | Java/Kotlin facade delegates lorem/email/ipv6 purely (no duplicated normalization/assembly to fix) | grep `src/main/java/org/galaxio/gatling/javaapi/FakerApi.scala` — all hits are `= Faker.x.y(...)` one-liners | verified | high | E004 | 2026-07-21 |
| V005 | `nextLongInclusive` is the shared substrate of `long`, `positiveLong`, `negativeLong`, and both `date.offset` overloads (feature 009) | Re-read Faker.scala:44-47,86-89,370-384 — offset overloads call `number.long(...)` which calls `nextLongInclusive` | verified | high | E005 | 2026-07-21 |
| V006 | CPF digit-Vector claim in #125 is stale; residual cost = `replaceFirst` compiling a Pattern per `formatted=true` call | Re-read Faker.scala:628-632 (stella `generateRandomValid()`); JDK `String.replaceFirst` javadoc: defined as `Pattern.compile(regex).matcher(str).replaceFirst(repl)` | verified | high | E006 | 2026-07-21 |
| V007 | No `Faker.java` exists anywhere (issue #123's path is stale; lorem lives in Scala core) | `find src examples -name Faker.java` → empty; lorem at Faker.scala:798-810 | verified | high | E007 | 2026-07-21 |
| V008 | `selectKeys` already hoists `keySet`; remaining per-record cost is `.view.filterKeys(...).toMap` (issue #129 premise partially superseded) | Re-read faker/Syntax.scala:96-99 | verified | high | E008 | 2026-07-21 |
| V009 | `withDefaults` semantics: record wins over defaults (`defaults.toMap ++ record` right-bias); hoisting `toMap` preserves it | Re-read Syntax.scala:102-103 + existing test GeneratedFeederSpec:218-221 asserts currency stays "EUR" | verified | high | E009 | 2026-07-21 |
| V010 | PR #300 wide-range fix + its regression test ("correctly generate longs when range is wider than Long.MaxValue") pre-exist on the feature branch | `git merge-base --is-ancestor 23f2243 HEAD` → NOT ancestor; grep test name in branch's GeneratedFeederSpec → 0 hits; 23f2243 merged to origin/main ~43 min after branch cut and rewrote `nextLongInclusive` | refuted | high | E010 | 2026-07-21 |
| V011 | JMH harness exists (`FakerBenchmark` + `JmhPlugin`) and benchmark sources are excluded from coverage; floor is 75/66 | build.sbt:15 (JmhPlugin), :41-45 (75/66 + benchmark exclusion patterns); FakerBenchmark.scala present | verified | high | E011 | 2026-07-21 |
| V012 | MiMa gate available: `mimaPreviousArtifacts` configured | build.sbt:77 — `Set("org.galaxio" %% "gatling-picatinny" % "1.23.0")` + filters seq :86 | verified | high | E012 | 2026-07-21 |
| V013 | String-assembly sites match plan: lorem `Vector.fill(count).mkString(" ")` :805; ipv6 `Vector.fill(8)(hexString(4)).mkString(":")` :234; TIN Range-map + prepend :709-714 | Re-read all three ranges in Faker.scala at branch HEAD | verified | high | E013 | 2026-07-21 |
| V014 | Plan R4 assumption "keep `hexString(4)` calls in ipv6 rewrite" is sound | Read RandomDataGenerators.scala:41-71 — `randomString` = `Iterator.continually(Random.nextInt…).map.take.mkString` on SHARED `scala.util.Random`; `hexString` itself not `@deprecated` (only ID generators :163-184 are), but it is the legacy pre-Faker surface + allocation chain + RNG contention; user directive: do not use | refuted | high | E014 | 2026-07-21 |
